import styled from '@emotion/styled'

export const Title = styled.h1({
  fontSize: '1.5em',
  textAlign: 'center',
  color: '#a95320',
})

export const Wrapper = styled.section({
  padding: '4em',
  background: '#dbe0da',
})
